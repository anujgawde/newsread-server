// const limitedData: [] = this.data.slice(0, 10);

// const filteredData = limitedData.map((e: any) => ({
//   id: e.article_id,
//   datePublished: e.pubDate,
//   title: e.title,
//   description: e.description,
//   content: e.content,
//   imageUrl: e.image_url,
//   sourceIcon: e.source_icon,
//   sourceUrl: e.source_url,
//   sentiment: e.sentiment,
//   mediaReference: e.mediaReference,
// }));

// const articles = this.articlesRepository.create(filteredData);
// await this.articlesRepository.save(articles);
// return this.data;
